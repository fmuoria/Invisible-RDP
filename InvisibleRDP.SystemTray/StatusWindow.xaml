<Window x:Class="InvisibleRDP.SystemTray.StatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InvisibleRDP - Status" 
        Height="400" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="InvisibleRDP Host Status"
                   FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock x:Name="ServiceStatusText" 
                       FontSize="14" Margin="0,5"/>
            <TextBlock x:Name="ConsentStatusText" 
                       FontSize="14" Margin="0,5"/>
            <TextBlock x:Name="IpAddressText" 
                       FontSize="14" Margin="0,5"/>
            <TextBlock x:Name="MachineNameText" 
                       FontSize="14" Margin="0,5"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Active Sessions" Padding="10">
            <ListBox x:Name="SessionsListBox" 
                     FontFamily="Consolas"
                     FontSize="11"/>
        </GroupBox>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Refresh" 
                    Width="100" Height="30"
                    Margin="0,0,10,0"
                    Click="RefreshButton_Click"/>
            <Button Content="Close" 
                    Width="100" Height="30"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
